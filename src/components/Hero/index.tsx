import { Formulario, HeroTitulo, ContainerForm } from './styles'

const Hero = () => (
  <Formulario>
    <ContainerForm>
      <HeroTitulo>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitulo>
    </ContainerForm>
  </Formulario>
)

export default Hero
